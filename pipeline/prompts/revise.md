You are performing a targeted, surgical revision on `<previous_rewrite>` based on strict QA findings.

## Instructions:
1. **Targeted correction only.** Treat `<previous_rewrite>` as your base draft. Fix every item listed in `<issues>` (e.g., restoring dropped arguments, correcting altered meanings, eliminating archaic residue) without rewriting sections that are already accurate and well-phrased.
2. **Prevent revision drift.** Do not introduce new interpretations, stylistic shifts, or modern clichés while making corrections. Ensure restored material matches the tone and flow of the surrounding text.
3. **Verify against `<original>`.** Ensure that every missing fact, name, step in reasoning, or qualification referenced in `<issues>` is faithfully integrated.
4. **Output format.** Return only the complete, corrected Markdown text of the passage. No meta-commentary, diff tags, or explanations.
